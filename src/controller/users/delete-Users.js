import { users } from "./get-Users.js";

export const deleteUsers=(req, res)=>{
    // const filtered = i?.filter((element, index) => {
    //     return element.userName == "Bold";
    //   });
    
      users = filtered;
    
      res.send(filtered);
}